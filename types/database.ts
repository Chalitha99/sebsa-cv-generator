/**
 * Placeholder until Phase 2 (docs/06-phase-plan.md) migrates the real schema and this is
 * regenerated with `supabase gen types typescript --project-id <ref> > types/database.ts`.
 * Kept as a distinct export so nothing importing `Database` needs to change when that happens.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
